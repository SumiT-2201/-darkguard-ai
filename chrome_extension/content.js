// Content script runs on all pages
// This can be used later to highlight the dark patterns directly on the DOM
// For now, it simply injects an invisible listener.
console.log("DarkGuard ML Content Script injected.");
